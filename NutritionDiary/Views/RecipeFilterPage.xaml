<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.RecipeFilterPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Фильтр рецептов"
             BackgroundColor="#FFFBF5">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="25">

            <!-- Заголовок -->
            <Frame BackgroundColor="#FF9E6D"
                   Padding="20"
                   CornerRadius="15"
                   HorizontalOptions="Fill">
                <Label Text="🔍 Поиск рецептов"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center" />
            </Frame>

            <!-- Поиск по названию -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="📝 Поиск по названию"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#5D4037" />

                    <Frame BackgroundColor="#F8F8F8"
                           CornerRadius="12"
                           Padding="15,0"
                           HeightRequest="50"
                           HasShadow="False">
                        <Entry x:Name="SearchBar"
                               Placeholder="Введите название рецепта..."
                               PlaceholderColor="#D8BFD8"
                               TextColor="#5D4037"
                               BackgroundColor="Transparent"
                               FontSize="16"
                               HorizontalOptions="Fill"
                               VerticalOptions="Center" />
                    </Frame>
                </VerticalStackLayout>
            </Frame>

            <!-- Популярные категории -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="🍽️ Популярные категории"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#5D4037" />

                    <FlexLayout x:Name="CategoriesFlexLayout"
                                Wrap="Wrap"
                                JustifyContent="Start"
                                AlignItems="Start">
                        <!-- Категории будут добавляться динамически -->
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Фильтр по калорийности -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="⚡ Калорийность"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#5D4037" />

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto" RowSpacing="10">
                        <Label Grid.Row="0" Grid.Column="0" 
                               Text="{Binding Source={x:Reference CalorieSlider}, Path=Value, StringFormat='От: {0:F0} ккал'}"
                               FontSize="12"
                               TextColor="#8D6E63" />
                        <Label Grid.Row="0" Grid.Column="1" 
                               Text="{Binding Source={x:Reference CalorieSlider}, Path=Maximum, StringFormat='До: {0:F0} ккал'}"
                               FontSize="12"
                               TextColor="#8D6E63"
                               HorizontalOptions="End" />

                        <Slider x:Name="CalorieSlider"
                                Grid.Row="1" Grid.ColumnSpan="2"
                                Minimum="0"
                                Maximum="1000"
                                Value="1000"
                                MinimumTrackColor="#FF9E6D"
                                MaximumTrackColor="#F5E6E0"
                                ThumbColor="#FF9E6D" />

                        <Label Grid.Row="2" Grid.ColumnSpan="2"
                               Text="Выберите максимальную калорийность"
                               FontSize="10"
                               TextColor="#8D6E63"
                               HorizontalOptions="Center" />
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Дополнительные фильтры -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="🎯 Особые предпочтения"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#5D4037" />

                    <FlexLayout x:Name="PreferencesFlexLayout"
                                Wrap="Wrap"
                                JustifyContent="Start"
                                AlignItems="Start">
                        <!-- Предпочтения будут добавляться динамически -->
                    </FlexLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Время приготовления -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="⏱️ Время приготовления"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#5D4037" />

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" RowSpacing="10">
                        <Label Grid.Row="0" Grid.Column="0" 
                               Text="{Binding Source={x:Reference TimeSlider}, Path=Value, StringFormat='До {0:F0} минут'}"
                               FontSize="12"
                               TextColor="#8D6E63" />

                        <Slider x:Name="TimeSlider"
                                Grid.Row="1" Grid.ColumnSpan="2"
                                Minimum="5"
                                Maximum="120"
                                Value="120"
                                MinimumTrackColor="#4DB6AC"
                                MaximumTrackColor="#E0F2F1"
                                ThumbColor="#4DB6AC" />
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Кнопки действий -->
            <Grid ColumnDefinitions="*,*" 
                  ColumnSpacing="15"
                  Margin="0,10,0,0">

                <Button Grid.Column="0"
                        Text="✅ Применить"
                        BackgroundColor="#FF9E6D"
                        TextColor="White"
                        CornerRadius="12"
                        FontSize="16"
                        FontAttributes="Bold"
                        HeightRequest="55"
                        Clicked="OnApplyFiltersClicked" />

                <Button Grid.Column="1"
                        Text="🔄 Сбросить"
                        BackgroundColor="#8D6E63"
                        TextColor="White"
                        CornerRadius="12"
                        FontSize="16"
                        FontAttributes="Bold"
                        HeightRequest="55"
                        Clicked="OnResetFiltersClicked" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>