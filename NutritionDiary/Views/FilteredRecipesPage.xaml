<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.FilteredRecipesPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Результаты поиска">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="20">

            <!-- Заголовок с информацией о фильтрах -->
            <Label x:Name="ResultsTitle"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <!-- Счетчик результатов -->
            <Label x:Name="ResultsCount"
                   
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextColor="Gray" />

            <!-- Список рецептов -->
            <CollectionView x:Name="RecipesCollectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="White"
                               Padding="15"
                               CornerRadius="10"
                               HasShadow="True">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                                <!-- Название рецепта -->
                                <Label Grid.Row="0" Grid.Column="0"
                                       TextColor="Black"
                                       Text="{Binding Title}"
                                       FontSize="16"
                                       FontAttributes="Bold" />

                                <!-- Калории -->
                                <Label Grid.Row="0" Grid.Column="1"
                                       Text="{Binding CaloriesPerServing, StringFormat='{0} ккал'}"
                                       FontSize="12"
                                       TextColor="Green"
                                       FontAttributes="Bold" />

                                <!-- Описание -->
                                <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                       Text="{Binding Description}"
                                       FontSize="12"
                                       TextColor="Gray" />

                                <!-- Категория и БЖУ -->
                                <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                       Text="{Binding NutritionInfo}"
                                       FontSize="10"
                                       TextColor="DarkGray" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Сообщение если нет результатов -->
            <Label x:Name="NoResultsLabel"
                   TextColor="Gray"
                   Text="😔 Рецепты по вашему запросу не найдены"
                   FontSize="16"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   IsVisible="False" />

            <!-- Кнопка возврата -->
            <Button Text="← Назад к фильтрам"
                    BackgroundColor="Gray"
                    TextColor="White"
                    Clicked="OnBackClicked" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>