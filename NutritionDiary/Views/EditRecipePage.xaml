<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.EditRecipePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Редактировать рецепт">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="20">

            <Label Text="Редактирование рецепта"
                   FontSize="20"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <!-- Блок с фото -->
            <Frame BackgroundColor="White"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="📷 Фото рецепта"
                           TextColor="Black"
                           FontAttributes="Bold" />

                    <!-- Изображение -->
                    <Frame x:Name="ImageFrame"
                           BackgroundColor="LightGray"
                           Padding="0"
                           CornerRadius="10"
                           HeightRequest="200"
                           IsVisible="True"
                           HorizontalOptions="FillAndExpand">
                        <Image x:Name="RecipeImage"
                               Aspect="AspectFill"
                               HorizontalOptions="FillAndExpand"
                               VerticalOptions="FillAndExpand" />
                    </Frame>

                    <!-- Кнопки для работы с фото -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button x:Name="AddPhotoButton"
                                Grid.Column="0"
                                Text="📷 Изменить фото"
                                BackgroundColor="#2196F3"
                                TextColor="White"
                                FontSize="12"
                                Clicked="OnAddPhotoClicked" />

                        <Button x:Name="RemovePhotoButton"
                                Grid.Column="1"
                                Text="❌ Удалить фото"
                                BackgroundColor="#FF5722"
                                TextColor="White"
                                FontSize="12"
                                Clicked="OnRemovePhotoClicked" />
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Основная информация -->
            <Frame BackgroundColor="White"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="📝 Основная информация"
                           TextColor="Black"
                           FontAttributes="Bold" />

                    <Label Text="Название рецепта:"
                           TextColor="Black"/>
                    <Entry x:Name="TitleEntry" 
                           BackgroundColor="LightGray"
                           TextColor="Black"
                           Placeholder="Введите название рецепта" />

                    <Label Text="Категория:"
                           TextColor="Black"/>
                    <Picker x:Name="CategoryPicker"
                            BackgroundColor="LightGray"
                            TextColor="Black">
                        <Picker.Items>
                            <x:String>Завтрак</x:String>
                            <x:String>Обед</x:String>
                            <x:String>Ужин</x:String>
                            <x:String>Перекус</x:String>
                            <x:String>Десерт</x:String>
                            <x:String>Напиток</x:String>
                        </Picker.Items>
                    </Picker>

                    <Label Text="Описание:"
                           TextColor="Black"/>
                    <Editor x:Name="DescriptionEditor" 
                            BackgroundColor="LightGray"
                            Placeholder="Опишите ваш рецепт..."
                            TextColor="Black"
                            HeightRequest="80" />
                </VerticalStackLayout>
            </Frame>

            <!-- Пищевая ценность -->
            <Frame BackgroundColor="White"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="⚡ Пищевая ценность на порцию"
                           TextColor="Black"
                           FontAttributes="Bold" />

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto,Auto">
                        <Label Grid.Row="0" Grid.Column="0" Text="Калории (ккал):"
                               
                               TextColor="Black"/>
                        <Entry x:Name="CaloriesEntry" 
                               BackgroundColor="LightGray"
                               TextColor="Black"
                               Grid.Row="0" Grid.Column="1" 
                               Text="0" 
                               Keyboard="Numeric" />

                        <Label Grid.Row="1" Grid.Column="0" Text="Белки (г):"
                               TextColor="Black"/>
                        <Entry x:Name="ProteinEntry" 
                               BackgroundColor="LightGray"
                               TextColor="Black"
                               Grid.Row="1" Grid.Column="1" 
                               Text="0" 
                               Keyboard="Numeric" />

                        <Label Grid.Row="2" Grid.Column="0" Text="Жиры (г):"
                               TextColor="Black"/>
                        <Entry x:Name="FatEntry" 
                               BackgroundColor="LightGray"
                               TextColor="Black"
                               Grid.Row="2" Grid.Column="1" 
                               Text="0" 
                               Keyboard="Numeric" />

                        <Label Grid.Row="3" Grid.Column="0" Text="Углеводы (г):"
                               TextColor="Black"/>
                        <Entry x:Name="CarbsEntry" 
                               BackgroundColor="LightGray"
                               TextColor="Black"
                               Grid.Row="3" Grid.Column="1" 
                               Text="0" 
                               Keyboard="Numeric" />
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Ингредиенты -->
            <Frame BackgroundColor="White"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="🛒 Ингредиенты"
                           TextColor="Black"
                           FontAttributes="Bold" />

                    <Editor x:Name="IngredientsEditor" 
                            BackgroundColor="LightGray"
                            TextColor="Black"
                            Placeholder="Перечислите ингредиенты и их количество..."
                            HeightRequest="120" />
                </VerticalStackLayout>
            </Frame>

            <!-- Шаги приготовления -->
            <Frame BackgroundColor="White"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="👩‍🍳 Шаги приготовления"
                           TextColor="Black"
                           FontAttributes="Bold" />

                    <Editor x:Name="CookingStepsEditor" 
                            BackgroundColor="LightGray"
                            TextColor="Black"
                            Placeholder="Опишите шаги приготовления по порядку..."
                            HeightRequest="120" />
                </VerticalStackLayout>
            </Frame>

            <!-- Время приготовления -->
            <Frame BackgroundColor="White"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="⏱️ Время приготовления"
                           TextColor="Black"
                           FontAttributes="Bold" />

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto">
                        <Label Grid.Row="0" Grid.Column="0" Text="Общее время (минуты):"
                               TextColor="Black"/>
                        <Entry x:Name="CookingTimeEntry" 
                               BackgroundColor="LightGray"
                               TextColor="Black"
                               Grid.Row="0" Grid.Column="1" 
                               Text="30" 
                               Keyboard="Numeric" />
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Кнопки действий -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0"
                        Text="💾 Сохранить изменения"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        FontAttributes="Bold"
                        Clicked="OnSaveClicked" />

                <Button Grid.Column="1"
                        Text="❌ Отмена"
                        BackgroundColor="#9E9E9E"
                        TextColor="White"
                        Clicked="OnCancelClicked" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>