<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.AddCustomProductPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Создать свой продукт">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="20">

            <Label Text="Создание нового продукта"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <!-- Добавьте этот код перед кнопками действий -->
            <ActivityIndicator x:Name="LoadingIndicator"
                  IsVisible="False"
                  IsRunning="False"
                  Color="Green"
                  HorizontalOptions="Center" />
            <!-- Название продукта -->
            <Label Text="Название продукта:" />
            <Entry x:Name="ProductNameEntry"
                   Placeholder="Введите название продукта"
                   PlaceholderColor="#696969"
                   BackgroundColor="White"
                   TextColor="Black"/>

            <!-- Пищевая ценность на 100г -->
            <Label Text="Пищевая ценность на 100г:"
                   FontAttributes="Bold"
                   Margin="0,10,0,0" />

            <Frame BackgroundColor="#F5F5F5"
                   Padding="15"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto,Auto,Auto">
                        <Label Grid.Row="0" Grid.Column="0" Text="Калории (ккал):"
                               TextColor="#696969"/>
                        <Entry x:Name="CaloriesEntry" 
                               Grid.Row="0" Grid.Column="1" 
                               Text="0" 
                               TextColor="Black"
                               Keyboard="Numeric"
                               BackgroundColor="White" />

                        <Label Grid.Row="1" Grid.Column="0" Text="Белки (г):"
                               TextColor="#696969"/>
                        <Entry x:Name="ProteinEntry" 
                               Grid.Row="1" Grid.Column="1" 
                               Text="0" 
                               TextColor="Black"
                               Keyboard="Numeric"
                               BackgroundColor="White" />

                        <Label Grid.Row="2" Grid.Column="0" Text="Жиры (г):"
                               TextColor="#696969"/>
                        <Entry x:Name="FatEntry" 
                               Grid.Row="2" Grid.Column="1" 
                               Text="0" 
                               TextColor="Black"
                               Keyboard="Numeric"
                               BackgroundColor="White" />

                        <Label Grid.Row="3" Grid.Column="0" Text="Углеводы (г):"
                               TextColor="#696969"/>
                        <Entry x:Name="CarbsEntry" 
                               Grid.Row="3" Grid.Column="1" 
                               Text="0" 
                               TextColor="Black"
                               Keyboard="Numeric"
                               BackgroundColor="White" />
                    </Grid>
                </VerticalStackLayout>
            </Frame>

            <!-- Примеры для справки -->
            <Frame BackgroundColor="LightYellow"
                   Padding="10"
                   CornerRadius="5">
                <VerticalStackLayout Spacing="5">
                    <Label Text="💡 Примеры:" 
                           FontAttributes="Bold" 
                           TextColor="#696969"
                           FontSize="12" />
                    <Label Text="Яблоко: 52 ккал, 0.3г белка, 0.2г жира, 14г углеводов" 
                           TextColor="#696969"
                           FontSize="10" />
                    <Label Text="Куриная грудка: 165 ккал, 31г белка, 3.6г жира, 0г углеводов" 
                           TextColor="#696969"
                           FontSize="10" />
                    <Label Text="Овсянка: 389 ккал, 16.9г белка, 6.9г жира, 66г углеводов" 
                           TextColor="#696969"
                           FontSize="10" />
                </VerticalStackLayout>
            </Frame>

            <!-- Кнопки действий -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button x:Name="SaveButton"
                    Grid.Column="0"
                        Text="Сохранить"
                        BackgroundColor="Green"
                        TextColor="White"
                        Clicked="OnSaveClicked" />

                <Button x:Name="CancelButton"
                    Grid.Column="1"
                        Text="Отмена"
                        BackgroundColor="Gray"
                        TextColor="White"
                        Clicked="OnCancelClicked" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>