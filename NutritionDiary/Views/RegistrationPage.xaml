<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.RegistrationPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Регистрация"
             BackgroundColor="#FFFBF5">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="25">

            <!-- Заголовок -->
            <VerticalStackLayout Spacing="5" HorizontalOptions="Center">
                <Label Text="👤"
                       FontSize="35"
                       HorizontalOptions="Center" />
                <Label Text="Создать аккаунт"
                       FontSize="28"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       TextColor="#5D4037" />
                <Label Text="Заполните информацию о себе"
                       FontSize="14"
                       HorizontalOptions="Center"
                       TextColor="#8D6E63" />
            </VerticalStackLayout>

            <!-- Форма регистрации -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">

                <VerticalStackLayout Spacing="15">

                    <!-- Основная информация -->
                    <VerticalStackLayout Spacing="12">
                        <Frame BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="UsernameEntry" 
                                   Placeholder="Логин *"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="16"
                                   TextColor="#5D4037"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>

                        <Frame BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="PasswordEntry" 
                                   Placeholder="Пароль *"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="16"
                                   TextColor="#5D4037"
                                   IsPassword="True"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>

                        <Frame BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="EmailEntry" 
                                   Placeholder="Email"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="16"
                                   TextColor="#5D4037"
                                   Keyboard="Email"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>
                    </VerticalStackLayout>

                    <!-- Личная информация -->
                    <Grid ColumnDefinitions="*,*" 
                          ColumnSpacing="10"
                          RowDefinitions="*,*,*"
                          RowSpacing="10"
                          Margin="0,10,0,0">

                        <Frame Grid.Row="0" Grid.Column="0"
                               BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="AgeEntry" 
                                   Placeholder="Возраст *"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="14"
                                   TextColor="#5D4037"
                                   Keyboard="Numeric"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>

                        <Frame Grid.Row="0" Grid.Column="1"
                               BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="HeightEntry" 
                                   Placeholder="Рост (см) *"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="14"
                                   TextColor="#5D4037"
                                   Keyboard="Numeric"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>

                        <Frame Grid.Row="1" Grid.Column="0"
                               BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="CurrentWeightEntry" 
                                   Placeholder="Текущий вес (кг) *"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="14"
                                   TextColor="#5D4037"
                                   Keyboard="Numeric"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>

                        <Frame Grid.Row="1" Grid.Column="1"
                               BackgroundColor="#F8F8F8"
                               CornerRadius="12"
                               Padding="15,0"
                               HeightRequest="50"
                               HasShadow="False">
                            <Entry x:Name="DesiredWeightEntry" 
                                   Placeholder="Цель (кг) *"
                                   PlaceholderColor="#D8BFD8"
                                   BackgroundColor="Transparent"
                                   FontSize="14"
                                   TextColor="#5D4037"
                                   Keyboard="Numeric"
                                   HorizontalOptions="Fill"
                                   VerticalOptions="Center" />
                        </Frame>

                        <Label Grid.Row="2" Grid.ColumnSpan="2"
                               Text="* - обязательные поля"
                               FontSize="11"
                               TextColor="#8D6E63"
                               HorizontalOptions="Center"
                               Margin="0,5,0,0" />
                    </Grid>

                    <!-- Кнопки -->
                    <VerticalStackLayout Spacing="10" Margin="0,15,0,0">
                        <ActivityIndicator x:Name="LoadingIndicator"
                                          IsVisible="False"
                                          IsRunning="False"
                                          Color="#FF9E6D"
                                          HorizontalOptions="Center" />

                        <Button x:Name="RegisterButton"
                                Text="Создать аккаунт"
                                BackgroundColor="#FF9E6D"
                                TextColor="White"
                                CornerRadius="12"
                                FontSize="16"
                                FontAttributes="Bold"
                                HeightRequest="50"
                                Clicked="OnRegisterClicked" />

                        <Button Text="← Назад"
                                BackgroundColor="Transparent"
                                TextColor="#8D6E63"
                                FontSize="14"
                                BorderColor="#8D6E63"
                                BorderWidth="1"
                                CornerRadius="10"
                                HeightRequest="45"
                                Clicked="OnBackClicked" />
                    </VerticalStackLayout>

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>