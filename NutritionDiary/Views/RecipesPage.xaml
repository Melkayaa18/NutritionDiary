<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.RecipesPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Рецепты"
             BackgroundColor="#FFFBF5">

    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="25">

            <!-- Заголовок -->
            <Frame BackgroundColor="#FF9E6D"
                   Padding="20"
                   CornerRadius="15"
                   HorizontalOptions="Fill">
                <Label Text="🍳 Кулинарный гид"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="White"
                       HorizontalOptions="Center" />
                <Label Text="Идеи для здорового питания"
                       FontSize="14"
                       TextColor="White"
                       HorizontalOptions="Center" />
            </Frame>

            <!-- Рецепт дня -->
            <Frame BackgroundColor="#FFF3E0"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Grid ColumnDefinitions="*,Auto">
                        <Label Grid.Column="0"
                               Text="⭐ Рецепт дня"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="#E65100" />
                        <Label Text="🔥"
                               Grid.Column="1"
                               FontSize="16"
                               TextColor="#E65100" />
                    </Grid>

                    <Label x:Name="DailyRecipeName"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="#5D4037"
                           LineBreakMode="WordWrap" />

                    <Label x:Name="DailyRecipeDescription"
                           FontSize="14"
                           TextColor="#8D6E63"
                           LineBreakMode="WordWrap" />

                    <Button x:Name="DailyRecipeButton"
                            Text="📖 Открыть рецепт"
                            BackgroundColor="#FF9E6D"
                            TextColor="White"
                            CornerRadius="12"
                            FontSize="14"
                            FontAttributes="Bold"
                            HeightRequest="45"
                            Clicked="OnDailyRecipeClicked" />
                </VerticalStackLayout>
            </Frame>

            <!-- Основные действия -->
            <Grid ColumnDefinitions="*,*" 
                  ColumnSpacing="15"
                  RowDefinitions="*,*"
                  RowSpacing="15">

                <Button Grid.Row="0" Grid.Column="0"
                        Text="🔍 Поиск рецептов"
                        BackgroundColor="#4DB6AC"
                        TextColor="White"
                        CornerRadius="12"
                        FontSize="14"
                        FontAttributes="Bold"
                        HeightRequest="60"
                        Clicked="OnFilterRecipesClicked" />

                <Button Grid.Row="0" Grid.Column="1"
                        Text="📖 Мои рецепты"
                        BackgroundColor="#7986CB"
                        TextColor="White"
                        CornerRadius="12"
                        FontSize="14"
                        FontAttributes="Bold"
                        HeightRequest="60"
                        Clicked="OnMyRecipesClicked" />

                <Button Grid.Row="1" Grid.Column="0"
                        Text="➕ Добавить рецепт"
                        BackgroundColor="#FFB74D"
                        TextColor="White"
                        CornerRadius="12"
                        FontSize="14"
                        FontAttributes="Bold"
                        HeightRequest="60"
                        Clicked="OnAddRecipeClicked" />

                <Button Grid.Row="1" Grid.Column="1"
                        Text="💡 Случайный рецепт"
                        BackgroundColor="#A1887F"
                        TextColor="White"
                        CornerRadius="12"
                        FontSize="14"
                        FontAttributes="Bold"
                        HeightRequest="60"
                        Clicked="OnRandomRecipeClicked" />
            </Grid>

            <!-- Категории рецептов -->
            <Frame BackgroundColor="White"
                   Padding="25"
                   CornerRadius="20"
                   HasShadow="True">
                <VerticalStackLayout Spacing="15">
                    <Label Text="📂 Категории рецептов"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#5D4037" />

                    <!-- ВАЖНО: CategoriesLayout должен быть VerticalStackLayout -->
                    <VerticalStackLayout x:Name="CategoriesLayout" Spacing="12" />
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>