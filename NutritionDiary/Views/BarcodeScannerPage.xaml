<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="NutritionDiary.Views.BarcodeScannerPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Сканирование штрих-кода">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20" VerticalOptions="Center">

            <Label Text="📷 Сканирование штрих-кода"
                   FontSize="20"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Frame BackgroundColor="#E3F2FD"
                   Padding="20"
                   CornerRadius="15">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Как это работает:"
                           TextColor="Black"
                           FontAttributes="Bold"
                           HorizontalOptions="Center" />

                    <Label Text="1. Нажмите 'Сделать фото' 📸"
                           TextColor="Black"
                           FontSize="14" />
                    <Label Text="2. Сфотографируйте штрих-код"
                           TextColor="Black"
                           FontSize="14" />
                    <Label Text="3. Мы найдем продукт по коду 🕵️‍♂️"
                           TextColor="Black"
                           FontSize="14" />
                </VerticalStackLayout>
            </Frame>

            <Button Text="📸 Сделать фото штрих-кода"
                    BackgroundColor="#4CAF50"
                    TextColor="White"
                    FontAttributes="Bold"
                    CornerRadius="10"
                    HeightRequest="60"
                    Clicked="OnTakePhotoClicked" />

            <Button Text="📁 Выбрать из галереи"
                    BackgroundColor="#2196F3"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="60"
                    Clicked="OnPickPhotoClicked" />

            <!-- Область для предпросмотра фото -->
            <Frame x:Name="PreviewFrame"
                   BackgroundColor="#F5F5F5"
                   Padding="0"
                   CornerRadius="10"
                   HeightRequest="200"
                   IsVisible="False">
                <Image x:Name="PreviewImage"
                       Aspect="AspectFit" />
            </Frame>

            <!-- Кнопка анализа фото -->
            <Button x:Name="AnalyzeButton"
                    Text="🔍 Анализировать штрих-код"
                    BackgroundColor="#FF9800"
                    TextColor="White"
                    CornerRadius="10"
                    HeightRequest="50"
                    IsVisible="False"
                    Clicked="OnAnalyzeClicked" />

            <Button Text="❌ Отменить"
                    BackgroundColor="Red"
                    TextColor="White"
                    CornerRadius="10"
                    Clicked="OnCancelClicked" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>